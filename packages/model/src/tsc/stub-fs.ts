/** Browser stub for `typescript/unstable/fs`. */
export type FileSystem = never;

export function createVirtualFileSystem(_files?: unknown): FileSystem {
  throw new Error("TypeScript 7.0.2 TypeChecker is not available in the browser");
}
