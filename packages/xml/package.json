{
  "name": "@bld/xml",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "description": "XML model, type inference, diagram I/O, CS blocks, and runner abstraction",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc --noEmit && node scripts/check-xsd.mjs",
    "test": "vitest run"
  }
}
